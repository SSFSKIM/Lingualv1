# School Integration Tasks

Status: Active
Last updated: 2026-05-30
Owner: Engineering + Product

## Status legend

- `[ ]` not started
- `[-]` in progress
- `[x]` done
- `[!]` blocked / needs decision

This file lists **active and open work only.** Completed phases are summarized at the bottom; for implementation detail of shipped work, see `TECH_SPEC.md` and the plan docs under `docs/superpowers/plans/`.

## Doc upkeep

- [ ] Review docs with product lead and confirm post-beta school scope.
- [ ] Convert remaining open questions in `PRD.md` §13 into explicit architecture decisions in `TECH_SPEC.md`.

## Postgres school-domain migration

- [x] Accept persistence ADR: Firestore remains auth/profile/legacy; Postgres becomes school-domain system of record.
- [x] Add initial Postgres schema blueprint for school operations, compliance, practice sessions, learning events, and analytics.
- [x] Schema-fidelity review of `POSTGRES_SCHEMA.md` against the live Firestore writers; folded Tier-1 fixes (missing `lti_platforms`/`lti_sessions` tables, `organizations` inline fields, `enrollments` canvas fields), tightened compliance CHECK constraints, and added the "Backfill Normalization And ID Resolution" section.

### Pre-migration fixes (do before backfill code)

- [x] Add `metric.context_tag_signal` to `SUPPORTED_EVENT_TYPES` (`backend/services/practice_analytics.py:10`) — was emitted by `build_derived_learning_events` but absent from the set that gates `POST /events` (`curriculum_admin.py:527`). Fixed 2026-05-30; 90 backend tests green.
- [x] Reconcile the two org-type constants. Decision (2026-05-30): school-only tenancy. Removed `schools.py`'s local `ORGANIZATION_TYPES`; both `/api/schools` and the school-request path now share `database.ALLOWED_ORG_TYPES` (`{'school'}`). Postgres `organizations.type` CHECK narrowed to `('school')`.
- [-] Pre-backfill data scans. The first production backfill (2026-05-31) surfaced these empirically rather than via a separate fail-fast scan, and the per-row SAVEPOINT + the multi-role merge handle them gracefully (no whole-run abort): **duplicate active `memberships` per `(org_id, uid)`** → these are multi-role users (staff + student); `upsert_membership` now MERGES roles into the one `(org,uid)` row instead of failing (see LIMITATIONS #45). **Duplicate/empty `enrollments` per `(class_id, student_uid)`** → 37 malformed empty-`student_uid` docs + 16 `e2e_test` docs; documented Firestore-only junk. Legacy `organizations.status='inactive'` is remapped to `archived` by normalization. Still TODO as a standalone pre-flight: `canvas_connections` per `class_id` and duplicate active join `code` scans (those collections aren't in the 2a enrollment chain).

### Implementation track

- [x] **Skeleton increment landed (2026-05-30).** Inert SQLAlchemy + Alembic + Cloud SQL engine wired behind `RouteDeps`; runtime unchanged (still Firestore). 930 backend tests green, zero regression. Ships: `backend/db/sql.py` (lazy connector/TCP engine), `backend/db/models/` (20 baseline tables, PG18 `uuidv7()` on append-heavy), `backend/db/migrations/0001_baseline` (Alembic), `backend/db/repository/` (resolution helper + inert enrollment twin), `RouteDeps.sql_engine` provider, `make test-postgres` (gated), `docs/dev/postgres-local-setup.md`.
- [x] Choose first implementation stack: `SQLAlchemy` 2.x + `Alembic` + `pg8000` + `cloud-sql-python-connector` (ADR-0001).
- [x] Provision Cloud SQL for PostgreSQL 18 (2026-05-30) **+ baseline schema applied to the real instance (2026-05-31).** Instance `lingual` (connection name `lingu-480600:us-central1:lingual`), RUNNABLE, IAM auth on, public IP, `lingual` application database. Schema applied via `alembic upgrade head` (rev `0001_baseline`, 20 tables) through the Cloud SQL Python connector (env: `INSTANCE_CONNECTION_NAME`+`DB_USER=postgres`+`DB_NAME=lingual`+`DB_PASS`, plus `GOOGLE_CLOUD_QUOTA_PROJECT=lingu-480600` to dodge the ADC quota-project gotcha). DDL credential = a freshly-reset `postgres` password (one-time, ephemeral — nothing used the prior password since runtime is Firestore). Runtime is **still 100% Firestore**. Local `make test-postgres` still uses ephemeral Docker `postgres:18` (NEVER point its `DATABASE_URL` at the real instance — `setUpModule` drops all tables).
- [ ] **Cost / tier downgrade at trial-end (HARD deadline).** The `lingual` instance is a Cloud SQL **free-trial** instance (created 2026-05-30): `db-perf-optimized-N-8`, 8 vCPU, Enterprise Plus — free now, but trial instances are LOCKED (edition/machine-type changes are rejected). Decision: keep the free trial, then downgrade in place to `db-custom-1-3840` (Enterprise) AFTER the trial converts to a normal paid instance (downgrade works with data present; reconfigure + restart). RISK: when the trial ends it bills the EP rate (~$1-2K/mo) until downgraded — get the trial-end date from the Cloud SQL console banner and set a reminder a few days prior. Downgrade command (edition + tier ALONE; bundling other fields is rejected): `gcloud sql instances patch lingual --project lingu-480600 --edition=ENTERPRISE --tier=db-custom-1-3840`. Do NOT enable any EP-only feature (data cache) meanwhile or the downgrade balks. The 100 GB disk won't shrink in place (~$17/mo headroom; ignore).
- [x] Add migration tooling, connection settings, and developer setup docs.
- [x] Decide the coexistence-window write strategy. Decision (2026-05-30): `legacy_firestore_id`-resolving writes, no write-freeze, UUID PKs retained (TECH_SPEC §3.8a, POSTGRES_SCHEMA "ID resolution"). Resolution helper lives in the repository layer behind `RouteDeps`.
- [-] Build a Postgres repository layer behind `RouteDeps` without changing route contracts. Inert seam landed (resolution helper + enrollment twin, Option A flat `deps.db`-twin contract). Per-entity adapters + the delegating router are deferred to each route-family cutover increment.
- [x] Close the `lti.py:703` `get_assignment_ref().update(...)` ref leak (2026-05-30). Added `database.set_assignment_grade_config(assignment_id, grade_metric, grade_points)`; the LTI grade-config route now writes through it instead of touching the assignment ref. Added a positive grade-config test (the path was negative-only before). Unblocks the assignments-entity cutover. 932 backend tests green.
- [-] Add per-route-family read feature flags so a failed cut can toggle reads back to Firestore without redeploy (TECH_SPEC §3.8e). **`ReadRouter` BUILT + wired (2026-06-01) — see `READ_CUTOVER.md` §2.** `backend/db/read_router.py` wraps the `database` module on `deps.db` (overrides only cut-over readers; `__getattr__` passes everything else through → zero route edits), wired in `main.py` with a shared engine provider + a startup banner. Per-entity flags `READ_PG_{ORGANIZATIONS,MEMBERSHIPS,CLASSES,ENROLLMENTS}`, default OFF, 3-state (`0`/`shadow`/`1`), fail-open to Firestore; reuses the dual-write engine resolver + a latency-bounded Session (`SET LOCAL statement_timeout`). `READ_PG_ORGANIZATIONS` added to cloudbuild (substitution, default `'0'`). 18 router unit tests. Remaining per-entity adapters + the flag flips are the slices below.
- [x] Write Firestore-to-Postgres backfill (dry-run, write, parity modes) — **enrollment chain, slice 2a complete (2026-05-30).** Library `backend/db/repository/{normalization.py, backfill.py}`: `run_backfill` processes orgs->memberships->classes->enrollments parent-first, resolves FKs via `legacy_firestore_id`, applies the documented renames/value-remaps/type-coercions, is idempotent (upsert by `legacy_firestore_id`), isolates each row in a SAVEPOINT (`begin_nested`) so a bad row never aborts the run, rejects missing-id docs, records `warnings` for would-be-NULL optional FKs, and has a chain-aware dry-run (no writes). Plus `parity_report` (Firestore vs Postgres id-set diff) and the `migration_import_runs` ledger (`start_import_run`/`finish_import_run`). Orchestrator: `scripts/backfill_postgres_school_domain.py` (`--dry-run`/`--write`/`--parity`; reads Firestore, runs via the configured engine, records the ledger). Verified: Tier-1 (no DB) + gated Tier-2 on real PG18 (`make test-postgres`, 14 tests: full-chain FK/remaps, idempotent re-run, SAVEPOINT isolation, dry-run-writes-nothing, parity in-sync + flags-unmigrated, ledger). Built via workflow + adversarial review (caught 2 critical data-corruption bugs pre-commit). **Multi-role membership merge added 2026-05-31** (after the first production backfill): `upsert_membership` UNIONs roles into the existing active `(org,uid)` row instead of inserting a row the partial-unique index rejects, so a staff+student user collapses to one row with both roles (union on both the cross-doc-merge and legacy-id-update branches → order-independent re-runs); `run_backfill` counts a merge as `updated` + emits a warning; the dry-run predicts it. The live `shadow_create_membership` inherits the fix for free (it delegates to `upsert_membership`). +7 Tier-1 + 5 Tier-2 tests (Tier-2 now 48). See LIMITATIONS #45. **Other entities** (assignments/compliance/practice) are their own cutover increments, not slice 2a.
- [x] Move new low-risk school-domain writes to Postgres first: organizations, classes, enrollments, assignments. **(enrollments via 2b `DUAL_WRITE_ENROLLMENTS`; org/membership/class + hard-delete via 2c `DUAL_WRITE_SCHOOL_CHAIN` — both flag-gated OFF, code-complete & green; assignments deferred to their own entity slice).** **Parent-chain dual-write = slice 2c, gated on its own `DUAL_WRITE_SCHOOL_CHAIN` flag (independent of `DUAL_WRITE_ENROLLMENTS`; must be enabled + parity-verified FIRST so enrollment FKs resolve).** New seam `backend/db/dual_write_school_chain.py` reuses the shared fail-open `_run` (refactored `dual_write._resolve_engine` to be flag-agnostic so both families share it). Mirror strategy: CREATE paths reuse idempotent `backfill.upsert_*`; STATUS-FLIP paths (suspend/restore/remove) use a TARGETED UPDATE keyed by `legacy_firestore_id` (a partial-doc upsert would clobber NOT-NULL `name`/`type`). **2c-1 organizations DONE (2026-05-31):** `shadow_create_organization` + `shadow_suspend_organization` + `shadow_restore_organization`; `sql_engine=` opt-in on `database.create_organization/suspend_organization/restore_organization`; wired at `schools.py:197` + `lingual_admin.py` suspend/restore. 1008 backend + gated Tier-2 PG18 (`test_dual_write_school_chain_pg`, 5 tests: create lands, idempotent, suspend/restore round-trip without clobber, unresolved no-op, flag-off inert). Built via workflow + 3-lens adversarial review. **2c-2 memberships DONE (2026-05-31):** `shadow_create_membership` (idempotent upsert; UnresolvedParentError = quiet no-op when org absent) + `shadow_remove_membership` (targeted UPDATE status=removed/removed_by_firebase_uid, no INSERT so partial-unique index never threatened). Fixed `backfill.upsert_membership` to skip `primary_class_ids` on UPDATE (so 2c-3 ARRAY writes survive a backfill re-run). `sql_engine=` opt-in on `database.create_membership`/`remove_membership`; `approve_school_request` shadows org+membership via post-commit re-read (org first so the membership FK resolves). Wired: schools.py (3 sites), teacher_requests.py, lti.py, identity.py, lingual_admin.py approve+remove. 1014 backend + Tier-2 PG18 (34 total: +resolved-FK create, idempotent, org-absent no-op, remove flips status, partial-unique removed+active coexist, backfill preserves primary_class_ids). **2c-3 classes + primary_class_ids + invite-code DONE (2026-05-31):** `shadow_create_class` (upsert; class_teachers junction deferred), `shadow_add_primary_class`/`shadow_remove_primary_class` (resolve class->UUID then `array_append`/`array_remove` on `memberships.primary_class_ids`; `NOT = ANY` guard makes add idempotent like ArrayUnion), `shadow_update_org_invite_code`/`shadow_deactivate_org_invite_code` (targeted UPDATE, never upsert). Wired the LTI `ArrayUnion` bypass (identity.py) explicitly. `sql_engine=` opt-in on `create_class`/`add_primary_class_to_membership`/`remove_primary_class_from_membership`/`generate_teacher_invite_code`/`deactivate_teacher_invite_code`; wired schools.py, teacher.py, lti.py, integrations.py. 1023 backend + Tier-2 PG18 (40 total, +6: class FK-resolved create, array append/idempotent/unresolved-no-op/remove, invite-code generate+deactivate without clobber). **At end of 2c-3 the full parent chain is mirrored — `DUAL_WRITE_SCHOOL_CHAIN` can be enabled + parity-verified, then `DUAL_WRITE_ENROLLMENTS`.** **2c-4 hard-delete mirroring DONE (2026-05-31):** `shadow_delete_org_scope` wired into `deletion_requests.execute_deletion` (after the ledger/consent writes, org-scope only, fail-open). Faithful mirror of `ORG_SCOPE_COLLECTIONS`: DELETE the org's classes (enrollments cascade) + memberships, KEEP the org row (Firestore doesn't delete the org doc — so the soft-archive-vs-hard-delete fork was moot). Resolves LIMITATIONS #43(a) for org scope; student/class scope target non-mirrored collections so no shadow fires. 1028 backend + Tier-2 PG18 (43 total, +3: full-chain org delete removes children/keeps org, unresolved no-op, flag-off inert). **Slice 2c COMPLETE — the full org/membership/class chain + hard-delete is mirrored. Schema applied + 2a backfill committed against the real instance (2026-05-31). IAM DB auth prepped (Cloud SQL IAM user = the Cloud Run SA, DML-only grants) and `cloudbuild.yaml` wired (DB env + `DUAL_WRITE_SCHOOL_CHAIN` substitution). DUAL_WRITE_SCHOOL_CHAIN=1 is now DEPLOYED & VERIFIED LIVE on `lingual-app` (us-central1, rev 00036) via passwordless IAM — a class created through the live prod UI mirrored to PG (shadow_create_class) and its UUID landed in the teacher's primary_class_ids (shadow_add_primary_class). A frontend test OOM (an AppGamesPage.test unstable-dependency render loop, fixed by the frontend agent — NOT the dual-write) had blocked the deploy through 3 build attempts. NEXT: soak, then add `DUAL_WRITE_ENROLLMENTS=1` (cloudbuild substitution) + deploy + soak; re-run 2a backfill + parity before read-cutover to catch any cold-start stragglers (e.g. the pre-fix "cloudsql2" test class).** assignments are a later entity.
- [-] Add temporary dual-write only for Firestore-backed readers that have not yet been cut over, with cross-store compensation (outbox record or idempotency key — not best-effort sequencing). **Enrollments, slice 2b code-complete + flag-gated OFF (2026-05-31).** Seam: `backend/db/dual_write.py` — `shadow_create_enrollment` / `shadow_set_enrollment_status` / `shadow_lti_reactivate`, all FAIL-OPEN (gated on `DUAL_WRITE_ENROLLMENTS=1` + a configured engine; open/close their own Session; swallow + log, never raise into the live write). Firestore stays system of record and is written FIRST. Wired via an opt-in `sql_engine=None` param on `database.create_enrollment/deactivate_enrollment/reactivate_enrollment` (callers pass `deps.sql_engine`: join-code `schools.py`, teacher remove `teacher.py`, LTI `lti.py`→`identity.py` incl. the direct-`.update()` reactivation bypass). Strategy A (trust-backfill): only enrollments mirror; an unresolved class is a quiet coexistence no-op. Cross-store compensation = idempotent upsert by composite `legacy_firestore_id` (reuses `backfill.upsert_enrollment`), NOT best-effort sequencing. Latency capped three ways (`sql.py` `pool_timeout=3` + connect timeout on both paths; transaction-scoped `SET LOCAL statement_timeout` in the shadow txn). Migration scripts (`migrate_canvas_roster_decouple.py`, `migrate_legacy_enrollment_ids.py`) abort when the flag is set. Verified: 997 backend tests + gated Tier-2 PG18 (`test_dual_write_enrollments_pg`, 9 tests: resolved-FK create, CHECK-surviving remaps, idempotency, `updated_at` bump, LTI 3-field write, unresolved no-op, flag-off inert, parity in-sync). Built via workflow + 3-lens adversarial review. **2b LIVE & VERIFIED (2026-05-31):** `DUAL_WRITE_ENROLLMENTS=1` deployed on `lingual-app` rev 00037 (after SCHOOL_CHAIN). Verified via a real student join (code KCRWSK): enrollments 26->27, the new row mirrored with status/join_source + BOTH FKs resolved (class_id, and student_membership_id -> the membership the same join created via the parent-chain shadow). Both flags are cloudbuild substitutions. Reads still 100% Firestore. Parent-chain (slice 2c) + enrollments (2b) dual-write is COMPLETE. **Remaining (separate phases):** soak/monitor, re-backfill+parity before cutover, then READ CUTOVER by route-family, then retire Firestore writes (see LIMITATIONS #43/#45).
- [-] Cut route reads over by family. **Scoped + designed + adversarially reviewed (2026-06-01) — authoritative sequencing in `READ_CUTOVER.md`.** Candidates are ONLY the 4 dual-written entities (org/membership/class/enrollment); the **analytics family is BLOCKED** (practice_sessions/learning_events are modeled but NOT dual-written/backfilled — see the `[!]` item below). Ordered slices:
  - [x] **(1) enrollments-read-prereq** *(code-only, flag OFF; commit 189ec00)* — fixed defect **D1**: the enrollment twin emitted `class_id`/`student_membership_id` as PG UUIDs; read queries now JOIN classes+memberships and `_serialize` emits the parents' `legacy_firestore_id` (the §3.0 invariant). +D1 regression tests. (`count_org_students` rides the enrollments-flip slice (8).)
  - [x] **(2) organizations-read — FLIPPED TO PG-AUTHORITATIVE (2026-06-01, rev `lingual-app-00045-6gr`, `READ_PG_ORGANIZATIONS=1`, fail-open).** First read path off Firestore. Promotion evidence: clean shadow soak (25+ `get_organization` compares, 0 mismatched on rev 00044 post-`_norm`-fix), dual-write drop-rate 0, parity at the documented-junk baseline; verified PG-authoritative serving (8/8 valid responses, 0 fail-open fallbacks). cloudbuild `_READ_PG_ORGANIZATIONS` default bumped to `'1'` for durability; rollback = instant `--update-env-vars READ_PG_ORGANIZATIONS=shadow`. **History:** adapter + **shadow DEPLOYED & SOAKED on prod** (2026-06-01, rev `lingual-app-00040+`, `READ_PG_ORGANIZATIONS=shadow`). `organizations_read.get_organization` (full org shape, inverse `suspended_by_uid`/`restored_by_uid` renames, `school_admin_uids` omitted+allowlisted) + the `ReadRouter.get_organization` override. Verified end-to-end via 5 authenticated `GET /api/schools/teacher-invite-code` calls (school admin, org `fc53dfbc`): the router resolved the PG row + compared field-for-field; the **only** divergence was `teacher_invite_code_active: (None, False)` (Firestore-absent vs PG NOT-NULL default) — FIXED in the comparator's `_norm` (False≈absent). **Progress toward flipping `READ_PG_ORGANIZATIONS=1`:** (a) **all entity-atomic org adapters DONE** — `get_organization`, `get_org_by_teacher_invite_code`, `search_organizations`, `count_organizations_by_status`, and `list_organizations` (keyset on `(name_lower, legacy_firestore_id)` + derived `school_admin_uids`→`memberCount` from a grouped memberships subquery — defect **D3**; shadow-compares its `items` by id-set via the router's `extract` hook — per-item field parity e.g. `memberCount` is a noted follow-up) + router overrides + Tier-1 + gated-Tier-2 tests. (b) **router-bypass audit DONE — no production bypass:** every consumer already calls `deps.db.get_organization` (codebase injects `db=deps.db` through `suspended_org_guard`/`compliance`/LTI); only the guard's `db is None` fallback is a latent footgun for FUTURE callers (re-audit before flipping). (c) **shadow REDEPLOYED with all 5 readers (2026-06-01, rev `lingual-app-00042-mfn`, `READ_PG_ORGANIZATIONS=shadow`)** — the 4 added readers now soak on prod traffic too. NEXT: confirm a clean soak (`shadow-read … 0 mismatched`) across all 5 → flip `READ_PG_ORGANIZATIONS=1`. (Origin pushed to 24fdae2.)
  - [ ] **(3) organizations COUNT aggregations** — ride `READ_PG_ORGANIZATIONS`.
  - [-] **(4) memberships-read** — **ADAPTERS BUILT, flag OFF (2026-06-01, `READ_PG_MEMBERSHIPS`).** `memberships_read.py` routes the two raw-row readers: `get_membership` (raw doc shape; inverse renames `firebase_uid`→`uid`, `removed_by_firebase_uid`→`removed_by_uid`; `org_id`→org `legacy_firestore_id` via JOIN) + `get_user_memberships` (org-JOIN enrichment `orgName`/`orgType`/`orgId`-as-legacy, replicated `_membership_sort_key`, multi-role merge handled at the data layer) + router overrides + `_MEMBERSHIP_SHADOW_IGNORE` + main.py banner + cloudbuild substitution. 8 Tier-1 + gated Tier-2 PG tests; 1107 backend green. **D4 RESOLVED:** `list_org_memberships`/`list_school_admin_emails`/`list_lingual_admin_emails` stay on Firestore (membership⋈users hybrids — email/name irreducibly Firestore; unavoidable fan-out either way); `resolve_user_school_context` stays passthrough (role-guard remains Firestore-authoritative even at flag=1 — calls the in-module primitive). **D5 found:** `primary_class_ids` deferred to `[]` by the backfill (live add/remove path mirrors it as UUIDs) — serializer translates UUID→legacy, shadow allowlists the field; a reconciliation backfill + `parity_report` is the per-field-parity flip prereq (low blast radius — no `primaryClassIds` consumer). **NEXT:** deploy `READ_PG_MEMBERSHIPS=shadow` to soak the id-set parity (the role-guard parity that matters), then decide the flip per the 3 conditions in `READ_CUTOVER.md` §5.4.
  - [x] **(6) classes-read — FLIPPED TO PG-AUTHORITATIVE (2026-06-02, rev `lingual-app-00047-74z`, `READ_PG_CLASSES=1`, fail-open).** `classes_read.py` routes the four class-entity readers the now-backfilled junctions support: `get_class` (the keystone, 14 callers), `list_org_classes`, `list_teacher_classes`, `get_class_by_join_code` + router overrides + `_CLASS_SHADOW_IGNORE` + main.py banner + cloudbuild substitution. **D2 FIXED:** the serializer JOINs `organizations` and emits `org_id` as the legacy id (so `assignment_resolver.py:1289` `class_record['org_id'] == active_organization_id` authz holds + `build_class_summary.orgId` renders). Junction reconstruction: `teacher_membership_ids[]` from `class_teachers`→`memberships.legacy_firestore_id` (sorted; authz uses `in`), and `join_code`/`join_code_active`/`join_code_generated_at` from the MOST-RECENT `class_join_codes` row (Firestore keeps the code when deactivated — surfaced with `active=False`; `get_class_by_join_code` still filters `active` so an inactive code won't resolve a join). 10 Tier-1 + 5 gated Tier-2 PG tests (D2, junction JOINs, deactivated-code surfaced-but-not-resolvable, list filters, router '1' serves PG); 1118 backend green. **Shadow soak (rev 00046, 2026-06-01):** 100 compares, 0 mismatched across BOTH the point-get (`GET /api/teacher/classes/{id}/analytics`→`get_class`) and the list readers (`GET /api/teacher/dashboard`→`list_teacher_classes`/`list_org_classes`), 0 fail-open; the shadow deploy needed a full rebuild (rev 00045's image predated `52637f6`) and bumped `_READ_PG_MEMBERSHIPS` 0→shadow in the same commit (`d1a8b7f`) to preserve the live membership shadow. **FLIPPED to `=1` (2026-06-02, rev 00047)** via instant `gcloud run services update --update-env-vars`; cloudbuild `_READ_PG_CLASSES` default bumped shadow→'1' for durability. **Post-flip verified:** banner `class reads served from Postgres`, 24/24 PG-served with 0 fail-open, dashboard renders all 4 classes from PG. `get_class_by_join_code` not shadow-soaked (would need a real prod join) — accepted (single indexed lookup, Tier-2 tested incl. deactivated-code, all 22 active codes in PG, fail-open covers any miss). **Rollback:** instant `--update-env-vars READ_PG_CLASSES=shadow`. **Deferred to slice 7** (need the enrollments JOIN): `list_student_classes`, `list_org_classes_summary`.
  - [x] **(7) classes student/admin lists + (8) enrollments-read — FLIPPED TO PG-AUTHORITATIVE (2026-06-02, rev `lingual-app-00049-t5p`, `READ_PG_ENROLLMENTS=1`).** Post-flip verified: banner `enrollment reads served from Postgres`, 20/20 PG-served, 0 fail-open. org/class/enrollment reads now ALL PG-authoritative (memberships stays shadow by design). Enrollment-family readers routed in `read_router.py`: `get_student_class_enrollment`, `list_class_enrollments`, `list_student_enrollments`, `count_org_students`, `list_student_classes` — plus `list_org_classes_summary` (slice 7, rides `READ_PG_CLASSES`). `enrollments.py` gained `count_org_students` (one `COUNT(enrollments⋈classes)`, ENROLLMENT-status filter only — NO class-status filter, matching the Firestore aggregate); `classes_read.py` gained `list_student_classes` (the read-cut of the Firestore N+1 → `enrollments⋈classes` JOIN, full `get_class` shape) + `list_org_classes_summary` (curated admin shape, all statuses). **D1 already fixed** (slice 1); the `(class_id, student_firebase_uid)` UNIQUE constraint means the point-get subsumes Firestore's legacy-fallback scan (it matches on the FK columns, not the concatenated `{class}_{student}` doc id) → the deterministic-key audit is moot. **`list_student_classes` gates on BOTH `READ_PG_ENROLLMENTS` AND `READ_PG_CLASSES` (the WEAKER mode wins)** — it reads PG class rows via the JOIN, so codex flagged that an enrollments-only gate would keep serving PG class data after a class rollback; the weaker-of-two gate removes the runbook dependency. `_ENROLLMENT_SHADOW_IGNORE` = created_at/updated_at (clock skew). + main.py banner + cloudbuild `_READ_PG_ENROLLMENTS` substitution (default `'0'`). **Codex-reviewed (SHIP-WITH-FIXES, all addressed):** P1 unresolved-parent now fail-opens via a `_FALLBACK` sentinel (was authoritative None/[] → would deny practice-launch/roster); P2a `backfill.upsert_enrollment` preserves Firestore `created_at`/`updated_at` (roster `enrolledAt` keeps the true date; live shadow omits them → `now()`, unaffected); P2b the two-flag gate; P3 `list_student_classes` orders by enrollment `updated_at`. 20 Tier-1 + gated Tier-2 (`test_enrollments_read_pg.py` + a `list_org_classes_summary` case); 1138 backend green. **DONE 2026-06-02** (shadow-deploy rev 00048 → soak 100/0 → re-backfill+parity → flip rev 00049). The full relational read chain is off Firestore. **NEXT for the migration:** retire Firestore school-domain writes after a post-flip confidence window — but that also requires the analytics family (practice_sessions/learning_events, still blocked) + assignments to migrate first.
  - [x] **(A) assignments dual-write + read — FLIPPED TO PG-AUTHORITATIVE (2026-06-02, rev `lingual-app-00054-98l`, `READ_PG_ASSIGNMENTS=1`).** ANALYTICS_MIGRATION Slice A — assignments are the FK parent of practice_sessions/learning_events, so this unblocks Slice B/C. `get_assignment` + `list_class_assignments` now read Postgres (fail-open). Live sequence: Alembic `0002` (grade_metric/grade_points; applied as the `postgres` table OWNER — cloudsqlsuperuser can't `ALTER TABLE`, ownership-gated) → deploy rev 00050 (flags OFF) → `DUAL_WRITE_ASSIGNMENTS=1` rev 00051 (BEFORE backfill → no write gap) → unified parent-first backfill `--write` (assignments **40 inserted / 0 errors**) + `--parity` (assignments **`in_sync`, fs 40 = pg 40, 0 missing / 0 extra**; other collections at the #45 dirty baseline, `extra_in_pg=0`) → `READ_PG_ASSIGNMENTS=shadow` rev 00052. **Pre-flip proof = an OFFLINE analog of the get_assignment shadow over ALL 41 prod assignments** (`database.get_assignment` vs `assignments_read` via `read_router._diff`) → surfaced one real divergence, the **vestigial `mapping_id`** legacy field (39/40 carry a dangling ref to the removed curriculum-overlay collection; no consumer anywhere) → added to `_ASSIGNMENT_SHADOW_IGNORE` (rev 00053; see LIMITATIONS #47) → **re-verified 41/41 clean, 0 mismatched** → flip rev 00054. **Live verification:** a scaffold-free assignment created through the prod UI dual-wrote to PG (count 40→41, `intensity=balanced`, `task_type=custom_prompt`); PG-authoritative `get_assignment` rendered the student page correctly; **post-flip 0 fail-open**. Seams: `dual_write_analytics.py` (`shadow_create_assignment` idempotent upsert + `shadow_update_assignment_canvas_link` + `shadow_set_assignment_grade_config`), `assignments_read.py` (FK-inverting serializer), `backfill.upsert_assignment`, router overrides + the `also`-tuple + per-field `normalize` hook (`target_language_intensity`/`release_at`/`due_at`). **Codex-reviewed (SHIP-WITH-FIXES, all 5 addressed)** incl. P1: get_assignment cutover would drop LTI grade config → added the grade columns (Alembic `0002`). cloudbuild `_DUAL_WRITE_ASSIGNMENTS`/`_READ_PG_ASSIGNMENTS` both bumped to live state. Rollback instant via `--update-env-vars`. **Slice B (practice_sessions) is now unblocked.**
  - [x] **(B) practice_sessions dual-write — SHIPPED & LIVE in prod (2026-06-02, rev `lingual-app-00056-kmv`, `DUAL_WRITE_ANALYTICS_SESSIONS=1`; commits `2ca4a34` build → `c084f00` codex-fixes → `d0a82ac`/`f424928` rollout).** ANALYTICS_MIGRATION §5b.4. `create_practice_session`/`update_practice_session` thread `sql_engine=deps.sql_engine` → fail-open `shadow_create_practice_session` (1000ms) / `shadow_update_practice_session` (2000ms; rolling summary every turn + `session.ended` finalize) via the new `_run_with_timeout` in `dual_write_analytics.py`. `upsert_practice_session` (3-FK resolve, `student_uid`→`student_firebase_uid`); NOT in `_PIPELINE` — term-scoped via `scripts/backfill_practice_sessions_term.py`. Dormant reconciler `POST /internal/analytics/sweep-orphaned-sessions` (`analytics_internal.py`, shared-secret). **No Alembic migration** (models schema-complete; `events_synced_at` dropped). Full backend suite green (1185). **Codex review:** P2 FIXED (the §5b.2-#7 self-disable was premature — would silently drop the PG session UPDATE if `DUAL_WRITE_ANALYTICS_EVENTS` were set before Slice C's `shadow_write_turn`; removed, lands WITH Slice C); P1 ACCEPTED for beta (`statement_timeout` bounds the query not the checkout/connect → up to ~3s/10s tail under a PG incident, fail-open + data-safe, same as live read paths, off-hot-path move = §5b.3/GA); parity validated by codex. **Rollout DONE 2026-06-02:** deploy rev 00055 (flag '0', clean boot) → `--update-env-vars` flip rev 00056 (env verified, banner `DUAL_WRITE_ANALYTICS_SESSIONS=1`, 0 fail-open) → term backfill `--write` (**48 inserted/0 errors**) + `--parity` (**`in_sync`, fs 48 = pg 48, 0 missing/0 extra**) → cloudbuild default bumped 0→1. Rollback instant via `--update-env-vars …=0`. **Remaining (optional/next):** a forward live session-create through the route is not yet observed (no student traffic since flip; fail-open + write-path prod-proven via the backfill) — optional manual check = launch an assignment as a student, watch PG 48→49. **Reconciler ARMED but INERT** — to activate: create `INTERNAL_SCHEDULER_SECRET` secret, add to `cloudbuild --set-secrets`, provision one Cloud Scheduler HTTP job. §5b.5 ordering: SESSIONS soaked BEFORE the events flag (Slice C). See LIMITATIONS #48.
  - [-] **(C) learning_events dual-write — CODE-COMPLETE & COMMITTED (2026-06-03); flag HELD `'0'` pending Slice B soak (§5b.5).** ANALYTICS_MIGRATION §5b.4. `shadow_write_turn(session_firestore_id, events, session_updates)` in `dual_write_analytics.py`: ONE transaction per turn — the turn's events bulk-inserted via `insert(LearningEvent).on_conflict_do_nothing(legacy_firestore_id)` PLUS the session-summary/finalize UPDATE as the last statement, four FK parents resolved ONCE, fail-open at 2000ms. The §5b.2-#7 self-disable LANDED: `shadow_update_practice_session` now returns early when `DUAL_WRITE_ANALYTICS_EVENTS=1` (so a turn takes one pool checkout, not two — `shadow_write_turn` subsumes it). Route wiring (`curriculum_admin.py`): the events seam (`:579`) and the session-create `session.started` seam now capture the Firestore event id `create_learning_event` returns (was discarded) and pass `(id, payload)` to `shadow_write_turn`; `update_practice_session(..., sql_engine=…)` still called (its shadow self-disables when events on). Events term-scope backfill `scripts/backfill_learning_events_term.py` — **chunked bulk INSERT (2000/chunk), NOT SAVEPOINT-per-row (§3.3)** — + per-session COUNT parity (§3.4). Flag wired in `cloudbuild.yaml` (default `'0'`) + main.py startup banner (warns if events on without sessions on). Tests: `TestShadowWriteTurn` (one-txn ordering, resolve-once O(4), unresolved-parent drop, id-skip) + inverted the Slice B self-disable test; 1191 backend green. **Flip sequence when Slice B soak is clean (§5b.5):** `backfill_learning_events_term.py --write` + `--parity` (per-session COUNT `in_sync`) FIRST → `--update-env-vars DUAL_WRITE_ANALYTICS_EVENTS=1` → bump cloudbuild default 0→1. Rollback instant via `=0`.
  - [ ] **Live shadow-read parity harness** — `_shadow_compare` by VALUE/SHAPE incl. DERIVED outputs + counts (numeric tolerance == junk-row count), known-divergence allowlist (62 junk rows + 3 merged memberships). Promote `shadow`→`1` only after a clean 48–72h soak + fresh in-sync `backfill.parity_report` + dual-write shadow drop-rate ≈ 0.
- [x] **(5) classes-junction-backfill prereq (HARD) — COMPLETE (deployed + backfilled + parity-verified, 2026-06-01).** Junction writes LIVE on `lingual-app-00044-4dw` (`DUAL_WRITE_SCHOOL_CHAIN=1`). Reconciliation backfill `--write` run against the real instance → **all 27 PG classes have ≥1 `class_teachers` link; 22 `class_join_codes` rows all active; 0 classes with >1 active code** (the `one_active_per_class` invariant holds on real PG). Parity at the documented-junk baseline (orgs 30/28, memberships 130/125, classes 29/27, enrollments 80/27; `extra_in_postgres=0` everywhere — the 2 missing classes are documented dirty-data rows whose org doesn't resolve). **Implementation:** `upsert_class` now reconciles both junctions from the denormalized class doc: `reconcile_class_teachers` (resolve each `teacher_membership_ids[]` → membership UUID; convergent insert/delete; unresolved skipped+warned) + `reconcile_class_join_code` (one active row per class via the partial-unique index; deactivate-then-flush ordering so the unique-active slot is free before activating; keeps inactive history). `shadow_create_class` inherits the teacher reconcile; the live join-code lifecycle has `shadow_generate_class_join_code`/`shadow_deactivate_class_join_code` (reuse the same reconcile → byte-identical to backfill), wired on `database.generate/deactivate_class_join_code` via `sql_engine=` (teacher.py join-code routes pass `deps.sql_engine`). Confirmed no route bypass (teacher_membership_ids written only via `create_class`). Tier-1 backfill fakes extended for the junction queries + 6 gated Tier-2 PG tests (create populates teachers, dropped-teacher reconcile, unresolved skip, generate/regenerate keeps one active + inactive history, deactivate flips+keeps, absent-class no-op); 1107 backend green. **Deploy + backfill + parity all DONE (see the [x] summary above) → class reads (slice 6) are now UNBLOCKED.** **Codex review DONE (3rd-person, write-path scrutiny):** found a real P1 — `reconcile_class_join_code` freed only the same-class active slot, not the GLOBAL unique-active-code index (`class_join_codes_active_code_idx`), so a code held active by another class would raise an IntegrityError that `_run` swallows into a silent PG/Firestore divergence. **FIXED** (deactivate any other class's active row for that code before activating → converges instead of raising; +Tier-2 test). Codex's other two findings ACCEPTED as the fail-open contract, not bugs: (P1#2) a junction-flush failure rolls back the whole class shadow (atomic + re-backfillable + Firestore is SoR; the global-slot fix removes the dominant raise source); (P2) concurrent same-class composite-PK insert is contained by `_run`/SAVEPOINT and self-heals on re-run (live never creates a class_id twice).
- [ ] Build the `analytics_rollups` refresh worker (scheduled or write-triggered) as a separate decision before that table earns its place; until then analytics stay on the live Python aggregation path.
- [ ] Retire Firestore school-domain writes after route-family cutover and monitoring.

## Teacher onboarding (Plan 4 follow-ups)

- [ ] Replace in-memory org search rate limiter with a shared store (Redis / Firestore counter) when scaling to multi-replica.
- [ ] 7-day reminder email for stale pending teacher join requests (v1.5). **Product decision needed before launch.**
- [ ] Realtime status listener on `/signup/teacher/pending` (replace 30s polling, v1.5).
- [ ] Wrap teacher-join approve flow in a Firestore batch/transaction (v1.5). Introduces the project's first transactional path — plan it cross-cuttingly.
- [ ] Document `PUBLIC_BASE_URL` in `.env.example` and the deployment runbook.
- [ ] Top-level `try/except` wrapper around the main body of each route in `backend/routes/teacher_requests.py` (matches `school_requests.py` pattern; ensures Firestore transient errors return shaped JSON, not unformatted HTML 500).

## LMS / roster import

- [ ] Add manual CSV fallback if LMS setup is delayed.

## Lingual admin panel (Plan 5 follow-ups)

- [ ] `PATCH /api/lingual-admin/organizations/<orgId>` (org metadata editing) — v1.5.
- [ ] Realtime listener for org-detail audit feed (replace pagination, v1.5).
- [ ] Bulk export of org audit feed as CSV — v1.5.
- [ ] Internationalize Lingual admin panel UI (en-only in v1).
- [ ] Wire `school_request_reminder_to_lingual` once the outbox sweep gap (LIMITATIONS #20) is closed.
- [ ] Reminder email for inactive suspended orgs (≥30 days `suspended_until` in past with auto-restore disabled) — needs product decision before launch.
- [ ] Backfill top-level `country` from `location.country` for `school_requests` rows submitted before the country denormalization fix. One-shot script: query `school_requests` where `country` is missing AND `location.country` is non-empty; write `country` to each. Until run, the Requests page country filter matches only post-fix rows.
- [ ] Wire `make test-emulator` into CI so a missing composite index trips before deploy. Infrastructure exists (`backend/tests/test_firestore_indexes.py` + Makefile target). Requires Java runtime on CI agents + Firebase CLI; add as a separate job that runs alongside `make test-backend`.
- [ ] Share the school-type enum between FE and BE. Today both sides hand-code overlapping lists in TS and Python, and the drift was caught by review rather than the type system. Either (a) emit the Python enum as a generated TS const at build time, or (b) move both to a single JSON/YAML source-of-truth that both layers read.
- [ ] Delete orphan Firestore composite index on `enrollments` — see LIMITATIONS #35 for the targeted `gcloud` command.

## Legacy migration (Plan 6 rollout)

- [ ] Run backfill `--dry-run` on staging.
- [ ] Run backfill on staging (writes).
- [ ] Run backfill on production.
- [ ] Monitor `[backfill]` and `legacy_role_pick` log volume for 1 week post-launch.
- [ ] "Switch to learning mode" for teacher/admin-migrated users who want to learn — v1.5.
- [ ] Localize the modal copy (en + ko) — v1.5.

## Modality and cost controls

- [ ] Add org / class / assignment modality policies.
- [-] Track voice minutes and estimated cost per session.

## Analytics hardening

- [-] Capture estimated speaking time, turn counts, transcript-based MLU, target usage, and first-pass cost summary.
- [-] Strengthen semantic event detection with locale-aware pedagogical signal rules.

## Compliance counsel review

- [ ] Validate COPPA workflow assumptions with counsel.
- [ ] Validate FERPA vendor / school-official workflow assumptions with counsel.
- [ ] Validate state biometric-risk assumptions, including Illinois BIPA exposure, with counsel.

## Pilot readiness

- [ ] Recruit 5-10 co-design teachers.
- [ ] Create pilot feedback loop and weekly issue triage.
- [ ] Define beta support process for consent, roster, and integration issues.

## Locale content

- [ ] Author Tagalog (`tl-PH`) games/curriculum content. Locale is wired end-to-end (prompt config, locale list, pronunciation prompts) but `curriculaByLocale` in `AppGamesPage.tsx` has no `tl-PH` entry, so Tagalog learners get no minigame content. Same partial-content gap exists for non-French locales.

---

## Shipped to date (high-level)

For implementation detail, see `TECH_SPEC.md` and `docs/superpowers/plans/`.

- **Phase 1 — School foundation.** Auth + memberships, organizations/classes/enrollments domain, role-aware route guards, MembershipContext, Firestore rules.
- **Phase 2 — Onboarding & admin.** Class create + join-code, Canvas LMS roster sync (decoupled from enrollments 2026-04-21), Plan 3 admin org wizard, Plan 4 teacher join-org flow with admin approval, Plan 5 Lingual admin panel with org lifecycle (suspend/restore + audit + auto-restore scheduler), Plan 6 legacy role migration modal + backfill script.
- **Phase 3 — Content & assignments.** Canvas content picker, AI-assisted draft from source packets, manual advanced authoring, scaffold-free `custom_prompt` mode, scenario fields stored directly on assignments (legacy `curriculum_mappings` removed).
- **Phase 4 — Practice engine.** Assignment resolver, practice session bootstrap, assignment-aware realtime prompt assembly, text fallback when voice is blocked, interaction contract preview.
- **Phase 5 — Events & analytics.** `learning_events` schema, lifecycle/turn/feedback/target/error events, per-session summaries, rubric-dimension scoring, class/student/assignment analytics endpoints + UIs, dashboard filters.
- **Phase 6 — Compliance.** Voice gating + `voice_allowed` enforcement, `textFallbackEnabled` downgrade, pronunciation policy gating, disclosure logging on key endpoints, class-scoped bulk consent + audit export, Epic A guardian packets (issue/resend/cancel + secure-link decision), Epic B deletion requests (admin approve/execute/retry, sync worker), school-wide admin compliance dashboard.
- **Phase 7 (partial) — Pilot readiness.** Contextual onboarding hints, public `/compliance` page, Firestore rules emulator tests (`firebase-tests/`, 44 cases).
- **Realtime model.** Upgraded to `gpt-realtime-mini-2025-12-15` (May 2026); turn detection is `semantic_vad` eagerness=auto.
- **Outbox email infrastructure.** Firestore `outbox_emails/` + `send_outbox_email` Cloud Function, 10 templates wired (school-request approve/decline, teacher-invitation, teacher-join admin/approve/decline, org-suspended, org-restored, school-request-to-Lingual, email-verification-code).
- **Email verification at signup (2026-05-31).** New email/password signups must enter a 6-digit code (outbox→Resend) before using the app; gated by a `login_required` session flag. Google auto-verified, existing accounts grandfathered. See LIMITATIONS #46.

## Definition of done for beta entry — met

- Teacher can create or import a class. ✓
- Teacher can create an assignment from Canvas content or teacher-authored source material. ✓
- Student can launch assignment-aware practice. ✓
- Teacher can see class and student analytics tied to that assignment. ✓
- Voice access respects consent and retention policy. ✓
- Teacher routes are role-protected. ✓
- Firestore rules are no longer placeholder-only. ✓
