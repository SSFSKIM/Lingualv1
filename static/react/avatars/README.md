# Avatars

Place the Lingual VRM model at:

- `frontend/public/avatars/lingual-teacher.vrm`

It will be served at runtime as:

- `/avatars/lingual-teacher.vrm`

This repo intentionally does not include a default `.vrm` asset to avoid licensing issues.

